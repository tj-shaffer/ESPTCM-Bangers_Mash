// Removed: Atlassian OAuth login routes were abandoned — no tenant admin
// consent. Authentication is now app-managed email + password (POST /api/login
// in routes/invoke.ts). See DECISIONS.md ADR-008. This file is slated for deletion.
export {};
